{
  "name": "hightide-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for hightide.io",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "rm -rf _site && eleventy",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "download-assets": "node scripts/download-assets.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
