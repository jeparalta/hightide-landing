name: hightide-landing
static_sites:
  - name: web
    github:
      repo: YOUR_GITHUB_USER/hightide_landing
      branch: main
      deploy_on_push: true
    build_command: npm ci && npm run build
    output_dir: _site
    catchall_document: index.html
    routes:
      - path: /
